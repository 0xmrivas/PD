<article id="doc-cover">
    <div class="wrapper">
        <div class="upper">
            <p>{{course_year}}</p>
        </div>
        <div class="title">
            <h1>{{ cover_title | e }}</h1>
            <h2>{{ cover_subtitle | e }}</h2>
        </div>
        <div class="properties">
            <p>---------------------------------------------</p>
            <p id="author">{{ department | e }}</p>
            <p id="copyright">{{ institution | e }}</p>
            </address>
        </div>
    </div>
</article>

<style>

@page {
    size: A4;
    margin: 2.54cm;
    font-family: 'Source Sans Pro', sans-serif;
    background: none;
}

@page :first {
    margin: 0;
}

.wrapper {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: stretch;
    padding: 0 2cm;
    background-image: url('{{ cover_logo | to_url }}');
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    background-attachment: fixed;
    color: white;
    width: 21cm; /* Anchura de A4 menos márgenes */
    height: 28cm; /* Altura de A4 menos márgenes */
    max-width: 21cm;
    min-width: 21cm;
    max-height:28cm;
    min-height: 28cm;
}
.upper {
    flex: 0 0 2.8cm; /* 10% de 25cm */
    max-height: 2.8cm;
    min-height: 2.8cm;
    display: flex;
    align-items: center;
    justify-content: flex-end;
}
.title {
    flex: 0 0 19.6cm; /* 70% de 25cm */
    max-height: 19.6cm;
    min-height: 19.6cm;
    display: flex;
    flex-direction: column;
    justify-content: center;
}
.properties {
    flex: 0 0 5.6cm; /* 20% de 28cm */
    max-height: 5cm;
    min-height: 5cm;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;

    p {
        padding-left: 1cm;
        font-size: 11pt;
        margin-top: 0.4cm;
    }
}

body {
    font-family: 'Source Sans Pro', sans-serif;
}

p {
    text-align: justify;
}

table {
    border-collapse: collapse !important;
    width: 100% !important;

    tr:nth-child(even) {
        background-color: #FFE5EA;
    }

    th {
        background-color: #E93456 !important;
        color: white !important;
        font-weight: bold !important;
        padding: 10px !important;
        text-transform: uppercase;
    }

    th, td {
        vertical-align: middle !important;
        text-align: left !important;
        padding: 10px !important;
    }
}

</style>

